import 'package:flutter/material.dart';

///表头区域装饰
class TableHeaderDecorationWidget extends StatelessWidget {
  const TableHeaderDecorationWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

///表内容区域装饰
class TableContentDecorationWidget extends StatelessWidget {
  const TableContentDecorationWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
